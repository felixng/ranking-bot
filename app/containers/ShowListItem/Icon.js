import styled from 'styled-components';

const Icon = styled.div`
  position: relative;
  display: block;
  text-align: right;
  flex: 1 0 20px;
  font-size: 200;
  opacity: 1;
`;

export default Icon;